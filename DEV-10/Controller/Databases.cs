namespace Controller
{
    /// <summary>
    /// Enum Databases
    /// enum of databases
    /// </summary>
    public enum Databases
    {
        Addresses,
        Stocks,
        Producers,
        Products,
        Supplies
    }
}